
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    height: "100%",
  },
  text: {
    top: "50%",
    fontSize: 16,
  },
});

export default styles;